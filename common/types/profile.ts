import { Pet } from "common/types/pet.ts";

export type Profile = {
	name: string;
	age: number;
	email: string;
	phone: string;
	address: string;
	city: string;
	state: string;
	zip: string;
	country: string;
};